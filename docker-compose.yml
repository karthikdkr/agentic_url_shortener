services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:////app/data/urlshortener.db
      WORKFLOW_STORE_PATH: /app/data/workflows.json
      BASE_URL: http://localhost:8000
    volumes:
      - ./data:/app/data
